{{define "pickup"}}🤖 Picked up ({{.Kind}} flow). Branch: `{{.Branch}}`{{end}}

{{define "already-done"}}🤖 Already implemented — closing. {{.Reason}}{{end}}

{{define "needs-info"}}🤖 Not confident enough to implement (confidence {{.Score}}/100). Answer the numbered questions below in a comment, then remove the `{{.Label}}` label to re-queue:

{{.Feedback}}{{end}}

{{define "park"}}🤖 Parked as `{{.Label}}` — this issue will not be retried automatically.

Remove the `{{.Label}}` label to queue a fresh attempt — the preserved worktree, branch and logs are reused, so no work is lost.{{if .Guidance}}

{{.Guidance}}{{end}}{{if .Error}}

<details><summary>Error detail</summary>

````
{{.Error}}
````

</details>{{end}}{{end}}

{{define "pr-comment"}}🤖 PR: {{.URL}}{{end}}

{{define "pr-title"}}{{.Title}} (#{{.Number}}){{end}}

{{define "pr-body"}}Closes #{{.Number}}

Automated by loope ({{.Kind}} flow). Spec and plan, if any, are committed in this branch under docs/.{{end}}

{{define "guidance-usage-limit"}}Cause: Claude usage/rate limit. Re-queue once the limit resets.{{end}}

{{define "guidance-budget"}}Cause: hit the turn/budget ceiling mid-run. Raise the execute maxTurns/maxBudgetUSD if this recurs.{{end}}

{{define "guidance-network"}}Cause: network outage. Re-queue once connectivity is back.{{end}}

{{define "uat-section"}}{{.UATMarker}}

## 🤖 UAT checklist

{{.Checklist}}{{end}}
